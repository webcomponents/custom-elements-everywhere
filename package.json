{
  "name": "custom-elements-everywhere",
  "version": "1.0.0",
  "description": "Compare how different frameworks handle Web Components.",
  "scripts": {
    "install-all": "npm run install-angular && npm run install-preact && npm run install-react && npm run install-vue",
    "install-angular": "cd tests/angular-tests && npm install",
    "install-preact": "cd tests/preact-tests && npm install",
    "install-react": "cd tests/react-tests && npm install",
    "install-vue": "cd tests/vue-tests && npm install",
    "test": "npm run test-angular && npm run test-preact && npm run test-react && npm run test-vue",
    "test-angular": "cd tests/angular-tests && npm test",
    "test-preact": "cd tests/preact-tests && npm test",
    "test-react": "cd tests/react-tests && npm test",
    "test-vue": "cd tests/vue-tests && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robdodson/custom-elements-everywhere.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/robdodson/custom-elements-everywhere/issues"
  },
  "homepage": "https://github.com/robdodson/custom-elements-everywhere#readme",
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^1.0.1"
  },
  "devDependencies": {
    "tachyons": "^4.7.0"
  }
}
