{
  "name": "custom-elements-everywhere",
  "version": "1.0.0",
  "description": "Compare how different frameworks handle Web Components.",
  "license": "MIT",
  "scripts": {
    "install-all": "npm run install-angular && npm run install-preact && npm run install-react && npm run install-vue && npm run install-karma-plugins && npm run install-webcomponents",
    "install-angular": "cd tests/angular-tests && npm install",
    "install-preact": "cd tests/preact-tests && npm install",
    "install-react": "cd tests/react-tests && npm install",
    "install-vue": "cd tests/vue-tests && npm install",
    "install-karma-plugins": "cd tests/karma-plugins/karma-custom-html-reporter && npm install",
    "install-webcomponents": "cd tests/webcomponents && npm install",
    "test": "rm -rf docs/results && npm run test-angular & npm run test-preact & npm run test-react & npm run test-vue & wait; kill $(jobs -p)",
    "test-angular": "cd tests/angular-tests && npm test",
    "test-preact": "cd tests/preact-tests && npm test",
    "test-react": "cd tests/react-tests && npm test",
    "test-vue": "cd tests/vue-tests && npm test",
    "build": "cd docs && npm run build"
  },
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^1.0.1"
  },
  "devDependencies": {
    "marked": "^0.3.6"
  }
}
