{
  "name": "custom-elements-everywhere",
  "version": "1.0.0",
  "description": "Compare how different frameworks handle Web Components.",
  "license": "Apache 2.0",
  "scripts": {
    "install-all": "npm i && lerna bootstrap",
    "build": "rm -rf docs/libraries; lerna run build; cd docs && npm run build && echo 'Site built successfully!'",
    "start": "cd docs && http-server -o"
  },
  "devDependencies": {
    "http-server": "0.11.1",
    "lerna": "^2.11.0"
  }
}
