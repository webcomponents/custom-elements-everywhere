{
  "name": "dio-tests",
  "scripts": {
    "test": "cross-env LIBRARY_NAME=dio karma start",
    "build": "npm run test; DIST=../../docs/libraries/dio && mkdir -p $DIST && cp -r ./meta $DIST/meta && cp -r ./results $DIST/results"
  },
  "dependencies": {
    "dio.js": "8.2.3"
  }
}
