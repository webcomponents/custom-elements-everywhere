{
  "scripts": {
    "test": "cross-env LIBRARY_NAME=powjs karma start",
    "build": "npm run test; DIST=../../docs/libraries/powjs && mkdir -p $DIST && cp -r ./meta $DIST/meta && cp -r ./results $DIST/results"
  },
  "devDependencies": {
    "chai": "4.1.2",
    "cross-env": "5.1.3",
    "karma": "1.7.1",
    "karma-chrome-launcher": "2.2.0",
    "karma-firefox-launcher": "1.1.0",
    "karma-sourcemap-loader": "0.3.7",
    "karma-webpack": "2.0.9",
    "mocha": "4.1.0",
    "webpack": "3.10.0"
  },
  "dependencies": {
    "@webcomponents/webcomponentsjs": "1.1.0",
    "powjs": "^2.3.2"
  }
}
